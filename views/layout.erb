<!doctype html>
<html lang='en'>
<head>
	<meta charset='UTF-8'>
	<meta http-equiv='X-UA-Compatible' content='IE=edge'>
  <meta name='viewport' content='width=device-width, initial-scale=1'>
	<title>Clear bit</title>
	<link crossorigin="anonymous" href="<%= css_url %>" media="all" rel="stylesheet" />
  <link as="script" href="<%= js_url %>" />
</head>
<body>
<% flash.each do |name, msg| %>
                <div class='alert alert-<%= name %>'><%= msg %></div>
            <% end %>
	<%= yield %>
</body>

</html>
